# Hermes / NousResearch hermes-agent — reference pointer

> Reference material for future HAM adapter/context work. **Not** a source of shipped HAM truth.

## Contents

- **`hermes-agent-reference.txt`** — Full **Repomix** merge of the upstream hermes-agent codebase (single document; very large). Use for offline search, symbol discovery, and adapter planning. Regenerate upstream if you need a fresher snapshot.

This folder does not vendor Hermes as a submodule; the text dump is a **read-only mirror** for human/tool review.

## Why it exists

Hermes is a primary supervisory/critic reference in HAM’s architecture story. When designing bridges or comparing behavior, having a searchable corpus of the reference implementation is useful without implying HAM implements or wraps that codebase.
