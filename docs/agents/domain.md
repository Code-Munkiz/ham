# Domain Docs Layout (HAM Local Skills)

Use single-context layout for now:

- `CONTEXT.md` at repo root (only if/when intentionally created)
- ADRs at `docs/adr/`

Guidance:

- Do not create `CONTEXT.md` unless there is clear glossary value.
- Create ADRs only for hard-to-reverse decisions with meaningful trade-offs.
- Prefer concise domain terminology that improves consistency across code and docs.
