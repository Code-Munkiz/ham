# Issue Tracker (HAM Local Skills)

The issue tracker for these repo-local skills is GitHub Issues for this repository.

- Create/read/update issues via `gh` when issue automation is requested.
- Keep issue content scoped to requested work; do not perform deploy/runtime actions from issue management tasks.
- If GitHub CLI auth is unavailable, report that limitation and stop before guessing.
