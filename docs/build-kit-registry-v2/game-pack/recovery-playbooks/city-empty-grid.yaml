# CONCEPTUAL SCHEMA PILOT — not loaded at runtime
id: recovery.city-empty-grid
kind: recovery_playbook
name: Fix empty or invalid city grid
status: proposed
schema_version: "0.1"
version: "0.1.0-pilot"
description: Repair missing grid dimensions or empty cells array at city start.
non_template_statement: Recovery spec only — not auto-applied today.
max_attempts: 2
applies_to:
  - game.city-builder-lite
  - tag:city-builder-lite
depends_on:
  - mechanic.city-grid-state
repairs_validators:
  - validator.city-seeded-grid
steps:
  - id: step.seed_grid
    action: seed_grid
    detail: "5x5 or 6x6 grid with empty or lightly seeded cells"
