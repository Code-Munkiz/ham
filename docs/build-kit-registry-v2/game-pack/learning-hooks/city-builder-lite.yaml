# CONCEPTUAL SCHEMA PILOT — not loaded at runtime
id: learning.city-builder-lite
kind: learning_hook
name: City builder lite learning
status: proposed
schema_version: "0.1"
version: "0.1.0-pilot"
description: Hermes evolution signals for city builder lite — not wired at runtime.
non_template_statement: Learning hook schema only — Hermes deferred per ADR-0018.
applies_to:
  - game.city-builder-lite
  - tag:city-builder-lite
source_phase_owner: game.city-builder-lite
signal_categories:
  - seeded_grid_state
  - valid_building_placement
  - production_tick_mutates_resources
  - resource_bounds_respected
  - goal_result_detection
  - preview_boot_success
outcome_fact_fields:
  - preview_boot_success: boolean
  - empty_grid_on_start: boolean
  - invalid_placement_accepted: boolean
  - production_tick_no_op: boolean
  - goal_result_missing: boolean
  - dashboard_drift_detected: boolean
  - validator_failures: array of validator id
guidance:
  - "Bounded preview/validator evidence only; no secrets in prompts"
