# CONCEPTUAL SCHEMA PILOT — not loaded at runtime
id: validator.financial-table-semantics
kind: validator
name: Financial table semantics
status: proposed
schema_version: "0.1"

description: >
  Requires semantic readable financial tables.

non_template_statement: Conceptual validator only.

applies_to:
  - app.sales-ops-dashboard-core

validates_modules:
  - section.recovery-aging-buckets
  - section.sales-ops-responsive-structure
  - component.recovery-aging-table
  - component.semantic-financial-table

severity: blocking
runner: conceptual
check_type: conceptual

pass_conditions:
  - "Dense financial data uses semantic table structure"

fail_conditions:
  - "Div-soup pseudo-tables or unreadable dense grids appear"

recovery_playbooks:
  - recovery.inaccessible-financial-table
