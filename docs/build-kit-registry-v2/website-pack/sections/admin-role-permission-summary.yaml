# CONCEPTUAL SCHEMA PILOT — not loaded at runtime
id: section.admin-role-permission-summary
kind: section
name: Admin role and permission summary section
status: proposed
schema_version: "0.1"

description: >
  Display-only summary of illustrative roles and permission groupings for admin IA context.

non_template_statement: Section playbook only — not a checked-in RBAC editor.

applies_to:
  - app.admin-dashboard-core

depends_on:
  - section.admin-user-team-summary

binds_components:
  - component.role-permission-pill

guidance:
  - "Render roles/permissions as static summary labels only."
