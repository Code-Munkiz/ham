# CONCEPTUAL SCHEMA PILOT — not loaded at runtime
id: section.sales-ops-shell
kind: section
name: Sales ops shell section
status: proposed
schema_version: "0.1"

description: >
  Bounded sales operations shell with sidebar/topbar and one main overview surface.

non_template_statement: Section playbook only — not a checked-in shell template.

applies_to:
  - app.sales-ops-dashboard-core

depends_on: []

binds_components:
  - component.sales-ops-shell

guidance:
  - "Use one shell frame and keep navigation illustrative and honest."
  - "Main region anchors summary, performance, and recovery sections in scan order."
