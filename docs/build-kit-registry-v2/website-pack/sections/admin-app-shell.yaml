# CONCEPTUAL SCHEMA PILOT — not loaded at runtime
id: section.admin-app-shell
kind: section
name: Admin app shell section
status: proposed
schema_version: "0.1"

description: >
  Bounded static admin shell with sidebar, topbar, and one main overview surface.

non_template_statement: Section playbook only — not a checked-in shell template.

applies_to:
  - app.admin-dashboard-core

depends_on: []

binds_components:
  - component.admin-shell
  - component.admin-sidebar-nav
  - component.admin-topbar

guidance:
  - "Use one clear shell frame; avoid deep nested app routing"
  - "Sidebar and topbar are illustrative and honest, not deceptive fake app chrome"
  - "Main region anchors admin overview sections in scan order"
