# CONCEPTUAL SCHEMA PILOT — not loaded at runtime
id: section.saas-empty-loading-error-states
kind: section
name: SaaS empty/loading/error states section
status: proposed
schema_version: "0.1"

description: >
  Empty, loading, and error state examples for async-looking dashboard regions.

non_template_statement: Section playbook only — not a checked-in state machine component.

applies_to:
  - app.saas-dashboard-core

depends_on:
  - section.saas-upgrade-cta

binds_components:
  - component.activity-item
  - component.resource-list

guidance:
  - "Render empty/loading/error as visible cards for activity/resource surfaces"
  - "States are static/local; never imply live fetch/backend/API wiring"
  - "Keep copy short, actionable, and accessible"
