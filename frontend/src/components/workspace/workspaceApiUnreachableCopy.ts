/** User-facing explanation when GET /api/me fails at the fetch/network layer (no HTTP response). */
export const WORKSPACE_API_UNREACHABLE_USER_COPY =
  "Workspace API unreachable. HAM could not reach the API server from this browser. This can happen with VPN, firewall, browser extension, DNS, or intermittent network issues.";
