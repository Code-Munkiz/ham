litellm
reportlab>=4.0.0
sentry-sdk>=2.0.0
typer>=0.26.7
rich>=15.0.0
python-dotenv
pyyaml>=6.0.3
pydantic
fastapi
python-multipart
uvicorn[standard]
httpx
edge-tts>=7.2.8
google-cloud-firestore>=2.16.0
google-cloud-storage>=2.14.0
google-cloud-secret-manager>=2.20.0
google-cloud-run>=0.16.0
# Phase 2.5 — Cloud Tasks worker enqueue and GKE worker pod scheduling.
google-cloud-tasks>=2.16.0
google-cloud-container>=2.40.0
kubernetes>=29.0.0
pypdf>=4.0.0
python-docx>=1.2.0
openpyxl>=3.1.0
# Windows: ConPTY for /api/workspace/terminal
pywinpty>=2.0.1; sys_platform == "win32"
playwright
PyJWT>=2.8.0
cryptography>=42.0.0
# Claude Agent SDK — Cloud Run image installs this for readiness + gated smoke only (no broad execution).
claude-agent-sdk>=0.1.70,<0.2.0
# Hermes Agent CLI (Native Builder workspace lane; also HTTP gateway is separate):
#   Docker/Cloud Run: pinned in Dockerfile as hermes-agent==0.15.2 → /usr/local/bin/hermes
#   Local dev: pip install hermes-agent  OR  bash <(curl -fsSL …/hermes-agent/main/scripts/install.sh)
#   Git pin: pip install "git+https://github.com/NousResearch/hermes-agent.git@<commit>"
